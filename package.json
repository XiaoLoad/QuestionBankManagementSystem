{
  "name": "question-bank-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "db:init": "node db-init.js",
    "prestart": "node db-init.js",
    "start": "node server.js",
    "build:web": "cd frontend && yarn build",
    "build": "yarn build:web && node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "multer": "^2.1.1"
  }
}
